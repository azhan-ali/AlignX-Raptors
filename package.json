{
  "name": "alignx-raptors",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "next": "^15.3.1"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev"
  }
}
